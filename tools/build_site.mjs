import {readFile,writeFile,mkdir,cp,readdir,stat,rm} from 'node:fs/promises';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import {verifyPdfBuild} from './verify_pdf_build.mjs';
import {escapeHtml as esc,code,anchor,glyphSize,chartCode,printableSheets,chartChunks} from '../site/assets/model.mjs';
export const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
await verifyPdfBuild(ROOT);
const readJson=async name=>JSON.parse(await readFile(path.join(ROOT,name),'utf8'));
const catalogue=await readJson('resources/catalogue.json');
const proposal=await readJson('docs/proposal.json');
const presentation=await readJson('resources/chart-presentation.json');
const entries=[...catalogue.entries].sort((a,b)=>a.codePoint-b.codePoint);
const byCode=new Map(entries.map(entry=>[entry.codePoint,entry]));
const byId=new Map(entries.map(entry=>[entry.glyphId,entry]));
const projectIcon=byId.get('opposed-bowls-0-0');
const vacantCount=catalogue.blocks.reduce((count,block)=>count+block.end-block.start+1,0)-entries.length;
const repertoireSummary=`${entries.length.toLocaleString('en-US')} mapped characters${vacantCount?` · ${vacantCount} unallocated positions`:''}`;
const display=catalogue.displayOrder.map(id=>byId.get(id));
const repo='https://github.com/ostomachion/quintessential-latin';
const pdfNames=['quintessential-latin','quintessential-latin-extended-a','quintessential-latin-extended-b','quintessential-latin-catalogue','quintessential-latin-proposal'];
const blockPdf=block=>pdfNames[catalogue.blocks.findIndex(item=>item.id===block.id)];
const glyph=(entry,width=48,max=32)=>`<span class="glyph-wrap" data-form="${esc(entry.glyphId)}" data-italic="${entry.postures.includes('Italic')}"><span class="glyph" aria-hidden="true" style="--glyph-size:${glyphSize(entry,width,max).toFixed(2)}px">${String.fromCodePoint(entry.codePoint)}</span><span class="pending-label" title="Native Italic pending">Italic pending</span></span>`;
const scriptLink=(entry,width=48,max=32,cls='')=>`<a href="charts.html#${anchor(entry.codePoint)}" data-glyph="${esc(entry.glyphId)}" class="${cls}" aria-label="${code(entry.codePoint)} ${esc(entry.name)}">${glyph(entry,width,max)}</a>`;
const nav=[['index.html','Introduction'],['charts.html','Code charts'],['proposal.html','UCSUR proposal'],['downloads.html','Downloads']];
function template(file,title,description,body){return `<!doctype html>
<html lang="en" data-posture="Roman"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="${esc(description)}"><meta name="theme-color" content="#ffffff"><title>${esc(title)}${file==='index.html'?'':' · Quintessential Latin'}</title><link rel="icon" href="assets/favicon.svg" type="image/svg+xml"><link rel="stylesheet" href="assets/style.css">${file==='charts.html'?'<link rel="stylesheet" href="assets/chart-presentation.css">':''}<link rel="preload" href="fonts/QuintessentialSerif/QuintessentialSerif-Variable.woff2" as="font" type="font/woff2" crossorigin><script type="module" src="assets/app.mjs"></script></head><body data-page="${file.replace('.html','')}"><a class="skip-link" href="#main">Skip to content</a><header class="site-header"><a class="brand" href="index.html"><span class="brand-icon" aria-hidden="true">${glyph(projectIcon,30,52)}</span>Quintessential Latin</a><nav class="site-nav" aria-label="Main navigation">${nav.map(([url,label])=>`<a href="${url}"${url===file?' aria-current="page"':''}>${label}</a>`).join('')}</nav></header>
<div class="font-bar"><div class="font-bar-inner"><span class="font-bar-title">Quintessential Serif <span class="code">0.250</span></span><div class="weight-control"><label for="font-weight">Weight</label><input id="font-weight" type="range" min="400" max="700" step="1" value="400" aria-describedby="font-weight-value"><output id="font-weight-value" for="font-weight">400</output></div><label class="italic-control" for="font-italic"><input id="font-italic" type="checkbox">Italic</label><p id="font-status" class="font-status" role="status" aria-live="polite"></p></div></div>
<main id="main" class="main"><p class="print-settings">Quintessential Serif 0.250 · <span data-print-posture>Roman</span> <span data-print-weight>400</span> · Draft private-use assignments</p><noscript><p class="no-script-note">The complete reference is available below. JavaScript enables font controls, search, and character details.</p></noscript>${body}</main>
<footer class="site-footer"><p>Quintessential Latin · Josh Hufford · Reference font 0.250</p><div><a href="${repo}">Source</a><a href="${repo}/issues">Issues &amp; contact</a><a href="downloads.html#licenses">Licenses</a></div></footer>
<dialog id="character-dialog" class="character-dialog" aria-labelledby="dialog-name"><div class="dialog-top"><p id="dialog-code" class="code"></p><button id="close-dialog" class="close-dialog" type="button" aria-label="Close character details">Close ×</button></div><div id="dialog-glyph" class="dialog-glyph"></div><h2 id="dialog-name"></h2><p id="dialog-meta" class="dialog-meta"></p><div class="dialog-actions"><button id="copy-character" type="button" class="primary">Copy character</button><button id="copy-code" type="button">Copy code</button><a id="character-permalink" href="charts.html">Permanent link</a></div></dialog><div id="announcement" class="announcement" role="status" aria-live="polite"></div></body></html>`;}
function title(eyebrow,heading,lead){return `<div class="page-head"><p class="eyebrow">${eyebrow}</p><h1>${heading}</h1><p class="lead">${lead}</p></div>`;}
function chart(start,count=256,kind='screen'){
  const columns=count/16,screen=presentation.screen,isPrint=kind==='print';
  const width=isPrint?presentation.grid.cellWidth*4/3:screen.cellWidth;
  const maximum=isPrint?presentation.grid.glyphSize*4/3:screen.glyphSize;
  return `<table class="chart-grid" data-chart-kind="${kind}" data-columns="${columns}" data-start="${start}" data-end="${start+count-1}" style="--chart-columns:${columns}"><caption class="visually-hidden">${code(start)}–${code(start+count-1)}</caption><thead><tr><th aria-label="Final hexadecimal digit"></th>${Array.from({length:columns},(_,column)=>`<th scope="col">${((start+column*16)>>4).toString(16).toUpperCase()}</th>`).join('')}</tr></thead><tbody>${Array.from({length:16},(_,row)=>`<tr><th scope="row">${row.toString(16).toUpperCase()}</th>${Array.from({length:columns},(_,column)=>{
    const point=chartCode(start,row,column),entry=byCode.get(point);
    return entry?`<td data-codepoint="${point.toString(16).toUpperCase()}"><a class="chart-cell" href="#${anchor(point)}" data-code="${point}" data-glyph="${esc(entry.glyphId)}" aria-label="${code(point)} ${esc(entry.name)}">${glyph(entry,width-6,maximum)}<span class="code">${point.toString(16).toUpperCase()}</span></a></td>`:`<td class="vacant" data-codepoint="${point.toString(16).toUpperCase()}" aria-label="${code(point)} unallocated"></td>`;
  }).join('')}</tr>`).join('')}</tbody></table>`;
}
function names(block){
  const groups=[];
  for(const entry of entries.filter(entry=>entry.blockId===block.id)){
    let group=groups.at(-1);
    if(group?.familyId!==entry.familyId){group={familyId:entry.familyId,entries:[]};groups.push(group);}
    group.entries.push(entry);
  }
  return `<section class="names-section" aria-label="${esc(block.title)} character names"><h3 class="names-heading">Character names</h3><p class="names-font-legend"><span aria-hidden="true">?</span> Reference font unavailable. Reload to retry.</p><div class="names-list">${groups.map(group=>`<section class="name-family" data-family="${esc(group.familyId)}"><h4>${esc(catalogue.families.find(family=>family.id===group.familyId).title)}</h4><ol>${group.entries.map(entry=>`<li id="${anchor(entry.codePoint)}" class="name-entry" data-name-codepoint="${entry.codePoint.toString(16).toUpperCase()}"><a href="#${anchor(entry.codePoint)}" class="code" data-glyph="${esc(entry.glyphId)}">${entry.codePoint.toString(16).toUpperCase()}</a>${scriptLink(entry,21,15)}<a class="entry-name" href="#${anchor(entry.codePoint)}" data-glyph="${esc(entry.glyphId)}">${esc(entry.name)}</a></li>`).join('')}</ol></section>`).join('')}</div></section>`;
}
function chartSheet(sheet,columns,kind='screen'){
  return chartChunks(sheet,columns).map(({start,end})=>{
    const count=end-start+1;
    return `<section class="reference-sheet ${kind==='print'?'print-sheet':''}" data-sheet-start="${start}" data-continues-before="${start>sheet.block.start}" data-continues-after="${end<sheet.block.end}"><h3 class="sheet-heading"><span>${start.toString(16).toUpperCase()}</span><span>${esc(sheet.block.title)}</span><span>${end.toString(16).toUpperCase()}</span></h3><div class="chart-scroll" tabindex="0" role="region" aria-label="Code chart ${code(start)} to ${code(end)}">${chart(start,count,kind)}</div><p class="sheet-footer"><span>Quintessential Serif 0.250 · <span data-print-posture>Roman</span> <span data-print-weight>400</span></span><span>Draft private-use assignments</span></p></section>`;
  }).join('');
}
const structureFamilies=['reserved-forms','stems','arms','arches','bowls','double-bowls','bowled-spines','opposed-bowls'];
const illustrative=structureFamilies.map(id=>display.find(entry=>entry.familyId===id)).filter(Boolean);
const hero=(illustrative.length>=6?illustrative:display.filter((_,i)=>[0,7,17,35,59,81].includes(i))).slice(0,6);
function introduction(){return `<section class="intro-hero"><div><p class="eyebrow">A constructed Latin script</p><h1>Quintessential Latin</h1><p class="lead">An extended Latin script built from uprights, branching strokes, bowls, and spines.</p><p>The reference font, Quintessential Serif 0.250, covers all 1,216 forms in both Roman and native Italic.</p><div class="inline-links"><a href="charts.html">Explore the code charts →</a><a href="proposal.html">Read the proposal</a></div></div><a class="project-emblem" href="charts.html#${anchor(projectIcon.codePoint)}" aria-label="${code(projectIcon.codePoint)} ${esc(projectIcon.canonicalName)}"><span class="emblem-glyph">${glyph(projectIcon,260,450)}</span><span class="emblem-caption"><span class="code">${code(projectIcon.codePoint)}</span><span>${esc(projectIcon.canonicalName)}</span></span></a></section>
<div class="meta-strip"><div><strong>1,216</strong><span>Mapped constructions</span></div><div><strong>3</strong><span>Character blocks</span></div><div><strong>400–700</strong><span>Variable weight</span></div></div>
<section class="section-rule"><div class="prose"><p class="eyebrow">The construction system</p><h2>Construction principles</h2><p>Uprights establish the vertical structure. Shoulders, hips, legs, and arms branch from it; bowls enclose or open space, while spines join changing directions. Additional arches, ascenders, and descenders expand the repertoire.</p></div><div class="structure-examples">${(illustrative.length?illustrative:hero).map(entry=>`<a class="structure-example" href="charts.html#${anchor(entry.codePoint)}" data-glyph="${esc(entry.glyphId)}">${glyph(entry,190,55)}<span class="example-name">${esc(catalogue.families.find(family=>family.id===entry.familyId)?.title||entry.canonicalName)}</span></a>`).join('')}</div><p class="prose">Character names describe visible structure. This reference assigns no pronunciation or language-specific meaning to the forms. Weight and posture are typographic choices applied to the same character.</p><div class="inline-links"><a href="downloads.html">Download fonts &amp; reference files</a></div></section>
<section class="section-rule prose"><p class="eyebrow">A proposed allocation</p><h2>Private-use allocation</h2><p>The draft UCSUR proposal records private-use assignments from <span class="code">U+F2A00</span> to <span class="code">U+F2EBF</span>. Publication here does not imply Unicode encoding, UCSUR registration, or acceptance.</p><p class="byline">Designed and maintained by Josh Hufford.</p></section>`;}
function charts(){return `<div class="chart-page-heading"><h1>Code charts</h1><p>Quintessential Latin · Version 0.250</p></div><div class="chart-tools"><nav class="block-jump" aria-label="Jump to a block">${catalogue.blocks.map(block=>`<a href="#${block.id}">${esc(block.title)}</a>`).join('')}</nav><div class="search-area"><div><label for="character-search">Find a character by name or code</label><input type="search" id="character-search" placeholder="e.g. bowl or U+F2A00" autocomplete="off" spellcheck="false" aria-controls="search-results"></div><p class="search-meta" id="result-count" role="status">${repertoireSummary}</p></div><section id="search-results" class="search-results" aria-label="Search results" hidden><ul class="result-list"></ul></section><p class="chart-intro">Select a character for its name, details, and copying. Columns give the first four hexadecimal digits; rows give the final digit.</p></div>${catalogue.blocks.map(block=>`<section class="chart-block" id="${block.id}"><div class="block-heading"><h2>${esc(block.title)}</h2><span class="code">${code(block.start)}–${code(block.end)}</span></div><div class="block-subhead"><p>${entries.filter(entry=>entry.blockId===block.id).length} characters · Draft private-use assignments</p><a href="pdf/${blockPdf(block)}.pdf">Download block PDF</a></div><div class="screen-grids">${printableSheets([block]).map(sheet=>`<div class="responsive-sheet">${[16,8,4].map(columns=>`<div class="chart-variant" data-chart-columns="${columns}">${chartSheet(sheet,columns)}</div>`).join('')}</div>`).join('')}</div><div class="print-grids">${printableSheets([block]).map(sheet=>chartSheet(sheet,16,'print')).join('')}</div><div class="chart-legend">${vacantCount?'<span><span class="legend-chip vacant"></span>Unallocated position</span>':''}<span>Arrow keys move between mapped cells</span></div>${names(block)}</section>`).join('')}`;}
function proposalPage(){return `${title('UCSUR draft proposal',esc(proposal.title||'A proposed private-use allocation'),esc(proposal.subtitle||'A documented repertoire of 1,216 constructions, with stable identities, canonical names, and an open reference font.'))}<p class="proposal-status">${esc(proposal.status||'Draft · not submitted or registered')}</p><p class="byline">${esc(proposal.author||'Josh Hufford')} · Quintessential Serif 0.250 · <a href="pdf/quintessential-latin-proposal.pdf">Download proposal PDF</a></p><div class="proposal-layout"><nav class="proposal-toc" aria-label="Proposal sections"><ol>${proposal.sections.map(section=>`<li><a href="#${esc(section.id)}">${esc(section.title.replace(/^\d+[.)]\s*/,''))}</a></li>`).join('')}<li><a href="#references">References</a></li></ol></nav><article>${proposal.sections.map(section=>`<section class="proposal-section" id="${esc(section.id)}"><h2>${esc(section.title)}</h2>${(section.paragraphs||[]).map(paragraph=>`<p>${esc(paragraph)}</p>`).join('')}${section.bullets?.length?`<ul>${section.bullets.map(bullet=>`<li>${esc(bullet)}</li>`).join('')}</ul>`:''}</section>`).join('')}<section class="proposal-section" id="references"><h2>References</h2><ol>${(proposal.references||[]).map(reference=>`<li><a href="${esc(reference.url)}">${esc(reference.title)}</a></li>`).join('')}</ol></section></article></div>`;}
function downloads(){return `${title('Files & sources','Downloads','Install the reference font, keep a copy of the charts, or work from the independent source repository.')}<section><h2>Reference PDFs</h2><p>Version 0.250 · Roman 400 · Embedded fonts and vector text</p><table class="download-table"><thead><tr><th>Document</th><th>Contents</th></tr></thead><tbody>${catalogue.blocks.map(block=>`<tr><td><a href="pdf/${blockPdf(block)}.pdf">${esc(block.title)}</a></td><td>${code(block.start)}–${code(block.end)}</td></tr>`).join('')}<tr><td><a href="pdf/quintessential-latin-catalogue.pdf">Combined code charts</a></td><td>All three blocks, five chart sheets, complete names lists</td></tr><tr><td><a href="pdf/quintessential-latin-proposal.pdf">Draft UCSUR proposal</a></td><td>Repertoire, construction system, encoding, and implementation</td></tr></tbody></table></section><section><h2>Quintessential Serif</h2><p>Variable fonts cover weights 400–700. Roman and native Italic each cover all 1,216 characters. Install the TTF or OTF files, or use WOFF2 for the web.</p><table class="download-table"><thead><tr><th>Face</th><th>Download</th></tr></thead><tbody><tr><td>Roman variable · 400–700</td><td><a href="fonts/QuintessentialSerif/QuintessentialSerif-Variable.ttf" download>TTF</a> · <a href="fonts/QuintessentialSerif/QuintessentialSerif-Variable.woff2" download>WOFF2</a></td></tr><tr><td>Italic variable · 400–700</td><td><a href="fonts/QuintessentialSerif/QuintessentialSerif-Italic-Variable.ttf" download>TTF</a> · <a href="fonts/QuintessentialSerif/QuintessentialSerif-Italic-Variable.woff2" download>WOFF2</a></td></tr>${[['Regular','Roman 400'],['Bold','Roman 700'],['Italic','Italic 400'],['BoldItalic','Italic 700']].map(([file,label])=>`<tr><td>${label}</td><td><a href="fonts/QuintessentialSerif/QuintessentialSerif-${file}.otf" download>OTF</a> · <a href="fonts/QuintessentialSerif/QuintessentialSerif-${file}.woff2" download>WOFF2</a></td></tr>`).join('')}</tbody></table></section><section><h2>Data &amp; editable sources</h2><table class="download-table"><thead><tr><th>Resource</th><th>Purpose</th></tr></thead><tbody><tr><td><a href="data/quintessential-latin-allocation.json" download>Allocation · JSON</a></td><td>Authoritative character identities and construction data</td></tr><tr><td><a href="data/catalogue.json" download>Catalogue &amp; coverage · JSON</a></td><td>Canonical names, presentation order, and compiled availability</td></tr><tr><td><a href="data/names-list.txt" download>Character names · text</a></td><td>Numeric code order across the complete repertoire</td></tr><tr><td><a href="${repo}">Source repository ↗</a></td><td>Editable font masters, immutable donors, build tools, and tests</td></tr></tbody></table></section><section id="licenses" class="prose"><h2>Licenses &amp; attribution</h2><p>Original project code and documentation are provided under the <a href="LICENSE.txt">MIT license</a>. Font software is provided under the <a href="fonts/QuintessentialSerif/OFL.txt">SIL Open Font License 1.1</a>; preserve the font license and notices when redistributing it.</p><p>Quintessential Serif is based on STIX Two Text. This site uses Source Sans 3 for interface and code-chart typography under its <a href="fonts/SourceSans3/OFL.txt">SIL Open Font License</a>. STIX Two Text’s <a href="fonts/STIXTwoText/OFL.txt">license</a> and <a href="fonts/STIXTwoText/TRADEMARKS.txt">trademark notice</a> accompany the files. Authorship and source provenance are documented in the repository.</p><p>Report a problem or discuss the proposal through <a href="${repo}/issues">GitHub Issues</a>.</p></section>`;}
await Promise.all(pdfNames.map(name=>stat(path.join(ROOT,'output/pdf',`${name}.pdf`))));
const destination=path.join(ROOT,'dist');
if(path.dirname(destination)!==ROOT||path.basename(destination)!=='dist')throw new Error('Unsafe site output path');
await rm(destination,{recursive:true,force:true});
await mkdir(destination,{recursive:true});
await cp(path.join(ROOT,'site/assets'),path.join(destination,'assets'),{recursive:true});
const screen=presentation.screen,grid=presentation.grid;
await writeFile(path.join(destination,'assets/chart-presentation.css'),`/* Generated from resources/chart-presentation.json. */
body[data-page="charts"]{--chart-cell-width:${screen.cellWidth}px;--chart-cell-height:${screen.cellHeight}px;--chart-label-width:${screen.rowLabelWidth}px;--chart-code-size:${screen.codeSize}px;--chart-coordinate-size:${screen.coordinateSize}px;--chart-name-size:${screen.nameSize}px;--chart-name-leading:${screen.nameLeading}px;--chart-wide-width:${screen.wideWidth}px;--chart-inner-rule:${grid.innerRule}pt;--chart-outer-rule:${grid.outerRule}pt;--chart-hatch-spacing:${grid.hatchSpacing}pt;--chart-hatch-rule:${grid.hatchRule}pt}
@container chart (min-width:${screen.mediumWidth}px){.chart-variant[data-chart-columns="4"]{display:none}.chart-variant[data-chart-columns="8"]{display:block}}
@container chart (min-width:${screen.wideWidth}px){.chart-variant[data-chart-columns="8"]{display:none}.chart-variant[data-chart-columns="16"]{display:block}.sheet-heading{grid-template-columns:1fr auto 1fr}.sheet-heading span:nth-child(2){order:0;grid-column:auto;margin:0}body[data-page="charts"] .names-list{columns:2}}
@media print{body[data-page="charts"]{--chart-cell-width:${grid.cellWidth}pt;--chart-cell-height:${grid.cellHeight}pt;--chart-label-width:${grid.cellWidth/2}pt;--chart-code-size:${grid.codeSize}pt;--chart-coordinate-size:${grid.coordinateSize}pt;--chart-name-size:${presentation.names.fontSize}pt;--chart-name-leading:${presentation.names.leading}pt}}
`);
for(const family of ['QuintessentialSerif','STIXTwoText','SourceSans3']){const source=path.join(ROOT,'resources/fonts',family),target=path.join(destination,'fonts',family);await mkdir(target,{recursive:true});for(const file of await readdir(source)){if(/\.(ttf|otf|woff2)$/.test(file)||['OFL.txt','FONTLOG.txt','TRADEMARKS.txt'].includes(file))await cp(path.join(source,file),path.join(target,file));}}
await mkdir(path.join(destination,'data'));await mkdir(path.join(destination,'pdf'));
await cp(path.join(ROOT,'resources/catalogue.json'),path.join(destination,'data/catalogue.json'));
await cp(path.join(ROOT,'resources/quintessential-latin-allocation.json'),path.join(destination,'data/quintessential-latin-allocation.json'));
await cp(path.join(ROOT,'resources/NamesList.txt'),path.join(destination,'data/names-list.txt'));
for(const name of pdfNames)await cp(path.join(ROOT,'output/pdf',`${name}.pdf`),path.join(destination,'pdf',`${name}.pdf`));
await cp(path.join(ROOT,'LICENSE'),path.join(destination,'LICENSE.txt'));
const pages=[['index.html','Quintessential Latin','An independent reference for the Quintessential Latin script, its structure, code charts, and draft UCSUR proposal.',introduction()],['charts.html','Code charts','All 1,216 Quintessential Latin characters in Unicode-style code charts and numeric names lists.',charts()],['proposal.html','UCSUR proposal','Draft private-use allocation proposal for the Quintessential Latin repertoire.',proposalPage()],['downloads.html','Downloads','Download Quintessential Serif, code-chart PDFs, the UCSUR proposal, and open source data.',downloads()]];
for(const [file,heading,description,body]of pages)await writeFile(path.join(destination,file),template(file,heading,description,body));
await writeFile(path.join(destination,'.nojekyll'),'');
console.log(`Built ${pages.length} static pages; ${entries.length} characters, ${printableSheets(catalogue.blocks).length} print chart sheets, ${pdfNames.length} PDFs.`);
